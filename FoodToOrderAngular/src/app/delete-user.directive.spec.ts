import { DeleteUserDirective } from './delete-user.directive';

describe('DeleteUserDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteUserDirective();
    expect(directive).toBeTruthy();
  });
});
